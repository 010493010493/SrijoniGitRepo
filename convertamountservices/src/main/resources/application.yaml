server:
  port: 8600
  
spring:
  application:
    name: amount-service
    
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.int}
  
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka